<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="viewModelEvent"
            type="threeblindmice.setitup.viewmodel.EventViewModel"/>
    </data>
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/event_tile_container">
            <TextView
                android:id="@+id/event_start_time"
                android:layout_width="@dimen/event_time"
                android:layout_height="@dimen/event_time"
                android:text="@{viewModelEvent.start}" />
            <TextView
                android:id="@+id/event_end_time"
                android:layout_width="@dimen/event_time"
                android:layout_height="@dimen/event_time"
                android:text="@{viewModelEvent.end}"  />
        </LinearLayout>
        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">
            <TextView
                android:id="@+id/event_title"
                android:layout_width="@dimen/event_id"
                android:layout_height="@dimen/event_id"
                android:text="@{viewModelEvent.title}"/>


        </LinearLayout>



    </LinearLayout>

</layout>